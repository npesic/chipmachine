# Milestone: Text Mode

After achieving the first milestone, which is to play one music file from command line, the next milestone is this: Working text mode version of the player.

## First goal

First goal is to update the plan with details what are the missing steps. It could be required to explore why the current behaviour when the text mode is invoked for the app is to hangover.

Example of text mode invocation: `./cm -X`
